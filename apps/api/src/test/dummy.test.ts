// ─── apps/api/src/test/dummy.test.ts ─────────────────────────────────────────

import { describe, it, expect } from 'vitest'

describe('Dummy Test – API', () => {
  it('works', () => {
    expect(true).toBe(true)
  })
})
